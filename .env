# Application
APP_ENV=production
APP_DEBUG=false
APP_URL=https://match3onlinerewards.gamer.gd
DOMAIN=match3onlinerewards.gamer.gd
FORCE_HTTPS=true
TIMEZONE=UTC
LOG_LEVEL=info

# PHP / App DB (MySQL)
# You can either set DB_DSN directly, or set host/db/user/pass separately.
DB_DSN=mysql:host=DB_HOST;dbname=DB_NAME;charset=utf8mb4
DB_HOST=DB_HOST
DB_PORT=3306
DB_NAME=DB_NAME
DB_USER=DB_USER
DB_PASS=DB_PASS

# App settings
JWT_SECRET=replace_with_a_very_long_random_string
JWT_TTL=604800                    # 7 days in seconds
MIN_PASSWORD_LENGTH=8
INITIAL_BALANCE=0.00
CURRENCY=USD
MIN_PAYOUT=1.00

# PayPal REST (preferred)
PAYPAL_ENV=sandbox                 # 'sandbox' or 'live'
PAYPAL_CLIENT_ID=                  # put your PayPal REST client_id here (server only)
PAYPAL_CLIENT_SECRET=              # put your PayPal REST client_secret here (server only)
PAYPAL_WEBHOOK_ID=                 # webhook id from PayPal (sandbox or live depending on PAYPAL_ENV)
PAYPAL_API_BASE_SANDBOX=https://api-m.sandbox.paypal.com
PAYPAL_API_BASE_LIVE=https://api-m.paypal.com
PAYPAL_TOKEN_CACHE_FILE=paypal_token_cache.json

# PayPal Classic (if you still need API Username/Password/Signature)
PAYPAL_CLASSIC_API_USERNAME=
PAYPAL_CLASSIC_API_PASSWORD=
PAYPAL_CLASSIC_API_SIGNATURE=

# Python microservice (Flask)
PY_MS_HOST=0.0.0.0
PY_MS_PORT=5000
PY_MS_API_KEY=replace_with_strong_api_key   # protect /payout endpoint (server-to-server auth)
PY_MS_BASE_URL=https://payouts.match3onlinerewards.gamer.gd

# InfinityFree / FTP (for deployment; store securely)
FTP_HOST=ftpupload.net
FTP_USER=if0_40589601
FTP_PASS=
DEPLOY_PATH=/htdocs

# Webhook & site config
SITE_ADMIN_EMAIL=admin@match3onlinerewards.gamer.gd
WEBHOOK_URL=https://match3onlinerewards.gamer.gd/api/webhook.php

# SMTP (for notifications, password resets)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=notify@match3onlinerewards.gamer.gd

# Rate limiting & anti-fraud
RATE_LIMIT_REQUESTS_PER_MINUTE=60
MAX_PAYOUT_PER_DAY=1000.00
MAX_PAYOUT_PER_REQUEST=500.00

# Optional 3rd-party services
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=
SENTRY_DSN=

# SSL provider / auto-cert
SSL_PROVIDER=letsencrypt

# Misc: development helpers (do not enable in production)
ENABLE_DEBUG_TOOLBAR=false
ALLOW_DEV_ORIGINS=localhost,127.0.0.1

# Notes:
# - Replace all placeholders with real values on the server. Do NOT commit .env to version control.
# - Keep this file readable only by the app user (chmod 600).
# - For multi-server setups, store secrets in a secrets manager (Vault, AWS Secrets Manager, etc.)